First, download the sample text files:
  $ ruby word_count.rb

Then, create the word count view:
  $ ruby word_count_views.rb

(the indexing with take a few minutes, so be patient!)

Finally, run the queries:
  $ ruby word_count_query.rb

